export const enum ResponseType {
	RAW = 'raw',
	JSON = 'json',
	STRING = 'string',
	BUFFER = 'buffer'
}

export const enum RequestType {
	FILE = 'file',
	JSON = 'json',
	URL_ENCODED = 'url_encoded',
	RAW = 'raw'
}

export const enum FormDataKeys {
	CODE = 'code',
	FILE_NAME = 'file_name'
}
